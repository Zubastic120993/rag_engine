# pyproject.toml's [project.dependencies] is the single source of truth for
# direct dependencies -- this file is kept in sync with it by hand for
# tooling that expects a bare requirements.txt (e.g. `pip install -r`), and
# must never be edited to add a pin pyproject.toml doesn't also carry.
# requirements-lock.txt (generated via `uv pip compile`, hashed, every
# transitive dependency pinned) is the actual reproducibility mechanism --
# see F-11 in the audit vault for why a direct-only pin wasn't sufficient.
#
# === Core AI + RAG Framework ===
langchain==0.3.14
langchain-community==0.3.14
langchain-ollama==0.2.1
langchain-chroma==0.1.4
langchain-text-splitters==0.3.4
chromadb==0.5.23
# chromadb 0.5.23's telemetry client calls posthog the pre-6.0 way —
# posthog.capture(distinct_id, event, properties), 3 positional args.
# posthog 6.0.0 changed that signature to capture(event, **kwargs), which
# makes every chromadb call crash-and-log ("Failed to send telemetry event
# ...: capture() takes 1 positional argument but 3 were given"). Pin below
# the break. Remove this pin once chromadb ships a release built against
# posthog>=6.
posthog<6.0.0
ollama==0.3.1
tenacity==9.0.0
numpy>=1.26.0
PyYAML>=6.0

# === Local UI ===
gradio==5.43.1

# === PDF Handling ===
pypdf==6.0.0
cryptography==49.0.0

# === Utilities ===
pydantic==2.9.2
python-dotenv==1.0.1
requests==2.32.3
tqdm==4.66.5
